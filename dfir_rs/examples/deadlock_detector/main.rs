// TODO: Deadlock detector example uses within-tick fixpoint cycles that require
// `loop {}` support in inline codegen.

fn main() {
    eprintln!("Deadlock detector example is temporarily disabled during inline codegen migration.");
}
