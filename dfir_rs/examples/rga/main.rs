// TODO: RGA example uses within-tick fixpoint cycles that require
// `loop {}` support in inline codegen. The flow functions have been
// updated to return InlineDfirErased, but the test assertions need
// updating once fixpoint support is added.

fn main() {
    eprintln!("RGA example is temporarily disabled during inline codegen migration.");
}
