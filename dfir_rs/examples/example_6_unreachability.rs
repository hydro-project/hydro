// TODO: Unreachability example uses within-tick fixpoint cycles that require
// `loop {}` support in inline codegen.

pub fn main() {
    eprintln!("Unreachability example is temporarily disabled during inline codegen migration.");
}
