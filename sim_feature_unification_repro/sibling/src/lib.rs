//! Intentionally empty. This crate exists only to pull `tokio` with the
//! `full` feature set into any cargo invocation that includes it, so that
//! feature unification changes how the *host* test binary of
//! `sim_repro_staged` is compiled. See `../README.md`.
